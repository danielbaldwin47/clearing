You are the primary implementation BUILDER for a Linux Rust disk scanner performance fix. Work in /home/diggle/Work/tui-disk. You are not alone in this codebase. Do not revert or edit others' files.

OWNERSHIP: You may edit src/scan.rs and scanner-specific tests only. You may build and run cargo test. Do not edit Cargo manifests, src/main.rs, src/delete.rs, src/theme.rs, src/ui.rs, src/ui/**, README, scripts, benchmarks, judging, progress, or fixtures. If a change outside scope is essential, report why without editing it. Visual pieces are already undergoing acceptance and must remain byte-for-byte untouched. Do not author benchmark scripts or modify independent benchmark parameters. Do not access judging answer keys or critic materials.

TASK: Inspect src/scan.rs and independent benchmark evidence benchmarks/full-tree-optimized.json and benchmarks/ui-full-tree-optimized.json plus benchmarks/README.md. Implement a substantive performance improvement that makes cold scans faster while preserving exact full-tree semantics and safety. Current release hash68ecd31121561f888f18ff36aa5a8ebbbb437d631f4dacdc9831f44af13c0547 loses broad cold cache 187.92ms vs gdu144.45ms and wins warm41.50vs48.54. Exact UI tree won cold3.11vs4.09 and warm1.14vs2.97. Earlier safe scanner without min_len8/batched progress/d_type hints was cold142.5vs135.3,warm32.2vs51.4, a real loss preserved in benchmarks/final.json. The new min_len8 grouping may impair scheduling/locality, but identify the cause yourself. Do not claim victory without independent measurement.

The benchmark runs isolated KVM Linux guest20vCPUs,ext4,160000files/7105dirs, real metadata cold caches dropped before each trial, warm scan after untimed fullscan. Gate is lower median and strict majority paired wins across7trials for both cache states. The independent author alone measures final performance; do not re-run to get lucky. You can use profiling tools or ordinary exploratory commands for diagnosis, but do not write any final benchmark/acceptance script.

PRESERVE: complete in-memory Node tree used by TUI; allocated blocks including directory blocks; apparent size; exact file/directory/error counts; hardlinks counted once; no traversal of symlink targets, including concurrent ancestor replacement; descriptor-relative nofollow scanner safety; cancellable interactive scans; arbitrary non-UTF8 Linux path names and unambiguous escaped display; errors surfaced rather than skipped. The public APIs and Node fields must remain compatible. Root scanner may resolve a root symlink once, while nested links remain separate.

Current source optimization: names() collects libc readdir CString+d_type hints; known directories openat(O_NOFOLLOW) then fstat (one metadata stat), others fstatat; rayon per-entry parallel iterator with_min_len8; progress atomic once per directory; display_path escapes invalid bytes. Consider clean ownership/parallel-work locality rather than weakening correctness or excluding difficult files. A directory-only task model and sequential in-directory metadata may be better, but you decide from evidence.

Implement now. Run cargo test and cargo build --release once after the final coherent change. Report exactly what you changed, causal performance rationale, test results, source and binary hashes, and any remaining risk. Keep your final response concise. Work headless. User's hard stop is2026-09-20 00:46:22UTC; current time~23:07UTC. You should return promptly when a coherent improvement is ready for independent measurement.
