You are the explicitly requested Fable builder for one bounded macOS test-fixture fix. You are not alone in the codebase: other agents are editing unrelated files. Preserve all existing production changes, including platform::identity and clear_errno. Your ONLY writable ownership is src/scan.rs in this isolated scratch workspace; the coordinator will verify and promote the result. Tools are limited to Read and Edit; do not run commands or write other files.

Current scanner fixture() returns a potentially noncanonical temp path, and large_directories_nested_trees_and_raw_names_are_complete unconditionally creates b"bad\xFFname\n". APFS requires valid UTF-8 filenames, so Mac cargo test fails. Make the fewest changes needed: (1) canonicalize the created fixture directory before returning it; (2) preserve invalid-byte filename creation/expectations on Linux, while macOS uses a valid Unicode plus control-character filename and checks that the expected escaped name and raw path are retained; keep wide/nested/scanned-entry/count checks on BOTH platforms; (3) if straightforward, add a pure display_path test using OsStr::from_bytes for invalid UTF-8/control/backslash escaping on BOTH platforms, without touching the filesystem. Do not change production logic.

Return a short summary of exact changes. The coordinator runs cargo test scan::tests and cargo check for aarch64-apple-darwin and x86_64-apple-darwin. No tests can be run by you because no command tool is available.