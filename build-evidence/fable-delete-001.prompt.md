You are the primary implementation BUILDER for a Rust Linux terminal disk explorer. Work in /home/diggle/Work/tui-disk. You are not alone in this codebase: another Fable builder is changing scanner internals while preserving public APIs and all Node fields. Do not revert others' changes.

OWNERSHIP: Edit only src/delete.rs and the deletion workflow portions of src/main.rs, plus deletion tests within those files. Do not change src/scan.rs, Cargo manifests, src/theme.rs, src/ui.rs, src/ui/foundation.rs, src/ui/view.rs, src/ui/confirm.rs, README, scripts, fixtures, judging, progress, or benchmarks. Winning visual files and confirmation must remain byte-for-byte identical. Do not access judging answer keys or critic materials. Do not author or modify independent acceptance/benchmark scripts.

TASK: Large recursive deletion blocks the TUI. Implement responsive deletion after the exact existing typed `delete` confirmation. Deletion runs on a scoped worker borrowing the existing scanned Node tree (no enormous full-tree clone), terminal stays responsive and draws progress reporting actual entries removed (never promises physically reclaimed bytes), Esc requests cancellation checked between removals, and completion reports success versus stopped/partial honestly. Always attempt a rescan after success, failure, or cancellation, restoring the prior directory when possible so displayed allocations reflect actual remaining filesystem contents. Handle a failed or cancelled post-delete rescan honestly: never silently render stale allocation as current or permit repeating unsafe actions on stale data; choose an explicit error/exit or safe retry mode if needed. Existing confirmation screenshot and key flow must remain identical.

SAFETY: Preserve existing descriptor-relative openat/unlinkat deletion, O_NOFOLLOW on root and each opened directory, device+inode validation of all traversed and removed entries, no deletion outside scanned root, refusal to cross filesystem boundaries, refusal to delete incompletely scanned directories, refusal when new unseen entries exist, and never follow symlinks. Do not weaken any checks or add artificial delay. Cancellation must be checked immediately before removal, between children, and as practical during traversal. All worker errors must stop deletion; cancellation cannot race UI return into continuing destructive work. If terminal/event errors occur, request cancellation before scoped join and return an honest error.

TESTS: Add meaningful deterministic tests for cancellation preserving not-yet-removed children and reporting exact removed-entry count, pre-cancel leaves all entries intact, cancellation after some removal is a partial outcome, existing no-follow/device/inode protections hold. Avoid flaky timing-only cancellation tests; an internal callback/test seam or deliberately counted progress hook is acceptable if production interface stays small. Preserve existing delete tests and run cargo test. Avoid cargo fmt across the repository; format only your two owned files. Do not run cargo build --release: the other builder coordinates one final combined release after you finish.

IMPLEMENT now using your tools. Parent is coordinator only: you must author all substantive code yourself. Report changed behavior, test counts, exact keyboard/progress/outcome string contract for the independent acceptance harness, and any safety concerns. Save no files outside your two owned source files. Headless only. Hard deadline 2026-09-20 00:46:22 UTC; now about 23:08 UTC. Return promptly when the bounded task is coherent.
