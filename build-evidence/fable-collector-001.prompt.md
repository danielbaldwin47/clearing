You are the PRIMARY FABLE BUILDER for a Rust Linux terminal disk explorer feature. Implement the feature now. Work in /home/diggle/Work/tui-disk, clean baseline commit176da86. You are not alone: root independently owns acceptance scripts and documentation. Do not revert or overwrite others' changes.

OWNERSHIP: You may create src/collector.rs and src/trash.rs and edit src/main.rs, src/ui.rs, and src/ui/*.rs only. Do NOT edit Cargo.toml/Cargo.lock, src/scan.rs, src/delete.rs, src/theme.rs, README, docs, scripts, judging, progress, fixtures, or git/config files. Do not inspect judging answer keys or critic materials. Root owns the release build: do not run cargo build --release. You may run cargo check, cargo test, cargo fmt/rustfmt (format only owned source files). No new dependency/crate. Tests may mutate only uniquely owned temporary data. Everything visual must stay headless.

USER FEATURE: Add a trash collector: select multiple files/folders across directories, review them, and move the batch to the Linux desktop Trash. Existing permanent deletion must continue working unchanged.

CONTROLS AND UI:
* SPACE toggles the highlighted file/folder into an in-session collector. Selection survives navigating, manual rescans, and App replacements after permanent deletion.
* c opens collector review. Arrows/jk move through rows; Space or Backspace removes the review row; Esc closes review. t opens batch Trash confirmation; type exact trash then Enter to confirm; Esc cancels. Empty collector is handled explicitly.
* Count and allocated total are always visible while browsing; collected entries have markers in map and list. Do not repurpose existing d/typed-delete: it remains the separate permanent-delete action.
* Review shows paths (all full paths, or a full-path detail for the selected row), sizes, total, keyboard controls, per-item errors and empty state.
* Explain clearly: Space is freed when Trash is emptied.
* A directory and its descendants must never be double-counted or double-submitted: adding a parent replaces collected descendants; attempting to add a child covered by a parent explains why.
* Preserve the current terminal style; use existing foundation/theme helpers. Can add --snapshot collector for deterministic headless inspection. The root will run actual headless visual capture.

MODEL / SAFETY:
* Own records of path, scanned device+inode identity, file type, allocated bytes, and scanned root/ancestor identities. Do not retain route indices as identity or clone the entire scan tree.
* Snapshot original identities when collecting; they remain stable across rescans. A newly replaced path must not become silently authorized just because the tree was rescanned.
* Preflight verifies scanned root, ancestor directories, and target identities/type. No symlink ancestors. Reject scan root, paths outside its root, and the system Trash itself (XDG_DATA_HOME/Trash or HOME/.local/share/Trash and mounted-volume .Trash / .Trash-uid locations; protect a collected ancestor that would contain Trash too). Keep raw OsString paths for syscalls and use the existing display-path escaping for UI.
* Use installed gio via std::process::Command arguments exactly equivalent to gio trash -- ABSOLUTE_PATH. No shell command construction. Missing gio, unsupported filesystems, permission errors or command failures must NEVER fallback to permanent deletion.
* Do not claim atomic race guarantees: validation followed by an external gio call necessarily has a race window. Preserve strong best-effort preflight and report the limitation honestly in your final response.
* Batch work runs on a worker thread with responsive progress and cancellation BETWEEN items. Do not abort a running gio process while it may be moving an item. Successful items leave the collector. Failed and unprocessed items remain with useful per-item error/status. Cancellation must retain not-yet-processed entries. Do not claim space freed merely because trash succeeded.
* Always rescan after mutation. Preserve collector ownership/state when App is replaced after batch trash, permanent deletion, and manual rescan. Stale/failed records remain explicit until user removes and deliberately recollects them. Do not silently refresh original identities.

EXISTING SEAMS (confirm against live files): App state in src/ui.rs; App::new replacements in src/main.rs around235/284 currently lose arbitrary state; introduce a preserve/rebuild helper. Modal dispatch is before ordinary browse controls around202/264. Main view list in src/ui/view.rs around91 and modal drawing around244. Existing post-delete rescan helper around449 can inform the batch workflow. Linux scanned Node.path/identity/raw filename data are already available; scan::display_path escapes non-UTF8 safely. Existing delete.rs provides fd-relative patterns but is outside your write scope.

VERIFICATION: Add meaningful unit tests for parent/child normalization, persistence of old identity across a rescan-like replacement, stale target refusal, symlink ancestor/root replacement refusal, outside/root/trash rejection, partial/error/cancel batch outcomes. Use a small injected command/executor seam so test failures/cancellation do not touch the real desktop Trash. At least one command-construction test may test argument boundaries for unusual filenames without spawning a destructive operation. Avoid exact-string-only tests that merely copy your UI output. Run cargo test and cargo check. Do not author or modify independent acceptance/benchmark scripts.

DELIVER: Implement all owned changes and report files changed, controls, APIs useful to independent tests, checks actually run, exact limitations, and any missing work. Be candid if a permission is denied: stop and report instead of bypassing permissions or switching providers. You have a 20-minute bounded handoff. Return when a coherent feature is implemented and tests pass; do not start unrelated cleanup or performance work.
