# Shared foundation frozen after judging

The builder declared this contract before state-specific judging. Its source dependencies are `src/theme.rs` and `src/ui/foundation.rs`; the referee monitors their combined fingerprint.

- Deep navy background, pastel category hues, and stable root-category hue families while drilling.
- Bright teal identifies focus. Coral identifies irreversible actions.
- Terminal monospace, bold names and byte amounts, muted supporting text.
- Rectangle area encodes allocated bytes, including directory allocation. All positive-byte remainders are represented or explicitly grouped.
- One level of nested rectangles previews descendants. Clipped labels use an ellipsis.

The fresh foundation critic chose the candidate over the strongest saved reference in a map-plus-legend comparison. The first-round investigation confirmed the bar was intact and the negative-control calibration had rejected the unfinished wireframe. The candidate's advantage was direct two-level labels and explicit size encoding. The critic also observed truncated labels, some small children without size text, a thin Documents sliver, dim unselected legend text, and unused canvas space. The referee counted one scoped foundation win under the user's updated rule; overview, navigation, deletion, performance and whole-interface acceptance remained separate.

The judged candidate was recaptured from release SHA256 `3e5d21588a4ce7c1c4f3123bec6722917565a19667a92cc9f5f979f9b0b9cbf0`; pixel comparison against the original judged screenshot was identical. The recapture manifest retains binary and source-file hashes. Subsequent scanner-only builds do not change this visual foundation fingerprint.
